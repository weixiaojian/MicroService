server:
  port: 3344

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/weixiaojian/MicroService/
          search-paths: microservicecloud-config
      label: master